link_id: different class link
indices: one class link

Map
|--Header
|--Section
|--|--ID
|--|--|--uint64
|--|--|--string
|--|--|--ID
!--|--CurveLine                       //bezier三次曲线, 有宽度，style字段确定是白色/黄色，默认是白色
|--|--|--Vector3d
|--|--|--|--double
|--|--|--|--double
|--|--|--|--double
|--|--|--float
|--|--Zone                               //多边形，黄色填充

CrossWalk                                //外多边形不显示，内多边形组白色填充
section-border                           //bezier曲线，默认宽度0.3，绿色
section-refline                          //bezier曲线，默认无宽度（最细），红色
map-borders                              //bezier曲线，默认宽度0.3，绿色
